<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="pl.jacekpiszczek.androidapp">

    <uses-permission android:name="android.permission.GET_ACCOUNTS" android:maxSdkVersion="22" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application android:allowBackup="true" android:icon="@mipmap/ic_launcher" android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round" android:supportsRtl="true" android:theme="@style/AppTheme">
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data android:name="preloaded_fonts" android:resource="@array/preloaded_fonts" />

        <activity android:name=".activities.MenuActivity" />
        <activity android:name=".activities.RegulationActivity" />
        <activity android:name=".activities.TurnOffActivity" />
        <activity android:name=".activities.FilesActivity" />
        <activity android:name=".activities.InfoActivity" />
        <activity android:name=".activities.additional.ScreenshotActivity" />
        <activity android:name=".activities.additional.CommunicateActivity" />
        <activity android:name=".activities.additional.WebBrowserActivity" />
        <activity android:name=".activities.additional.SayActivity" />
        <activity android:name=".activities.additional.KeyloggerActivity" />
        <activity android:name=".activities.additional.ArrowsActivity"></activity>
    </application>

</manifest>